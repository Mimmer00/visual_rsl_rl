[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-rslrl-ac"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["torch", "rsl-rl"]

[tool.setuptools]
package-dir = {"" = "src"}
